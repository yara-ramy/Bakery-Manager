<odoo>
    <record id="bakery_order_list_view" model="ir.ui.view">
        <field name="name">bakery.order.list</field>
        <field name="model">bakery.order</field>
        <field name="arch" type="xml">
            <list string="Your orders">
                <field name="customer_id"/>
                <field name="total_price"/>
            </list>
        </field>
    </record>
    <record id="bakery_order_form_view" model="ir.ui.view">
        <field name="name">bakery.order.form</field>
        <field name="model">bakery.order</field>
        <field name="arch" type="xml">
            <form string="Order">
                <sheet>
                    <div class="d-flex justify-content-end mb-3">
                        <button name="action_checkout"
                                string="Checkout"
                                type="object"
                                class="btn btn-primary"
                                invisible="status != 'cart'"/>
                        <button name="action_cancel"
                                string="Cancel"
                                type="object"
                                class="btn btn-secondary"
                                invisible="status not in ['confirmed','cart']"/>
                    </div>
                    <group>
                        <field name="customer_id"/>
                    </group>
                    <field name="order_line_ids">
                        <list editable="bottom">
                            <field name="item_id"/>
                            <field name="quantity"/>
                            <field name="unit_price"/>
                            <field name="subtotal"/>
                        </list>
                    </field>
                    <field name="total_price"/>
                </sheet>
            </form>
        </field>
    </record>
    <record id="cron_change_status" model="ir.cron">
        <field name="name">Change status of order</field>
        <field name="active" eval="True"/>
        <field name="interval_type">days</field>
        <field name="interval_number">2</field>
        <field name="model_id" ref="model_bakery_order"/>
        <field name="state">code</field>
        <field name="code">model.cron_change_status()</field>
    </record>
</odoo>